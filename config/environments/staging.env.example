# Staging matrix — examples only. Inject real values from Key Vault / GitHub Environment secrets.
MASMS_ENV=staging
MASMS_SECRET_BACKEND=key_vault
MASMS_KEY_VAULT_URI=https://REPLACE-staging-kv.vault.azure.net/
MASMS_DATABASE_URL=REPLACE_FROM_KEY_VAULT
MASMS_CORS_ORIGINS=https://staging.example.invalid
