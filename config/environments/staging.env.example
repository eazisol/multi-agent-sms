# Staging matrix — examples only. Inject real values from AWS Secrets Manager / GitHub Environment secrets.
MASMS_ENV=staging
MASMS_SECRET_BACKEND=secrets_manager
MASMS_AWS_REGION=us-east-1
MASMS_SECRETS_PREFIX=masms/staging
MASMS_DATABASE_URL=REPLACE_FROM_SECRETS_MANAGER
MASMS_CORS_ORIGINS=https://staging.example.invalid
