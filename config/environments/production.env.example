# Production matrix — examples only. Never commit real credentials.
# Human approval + GitHub Environment protection required before deploy.
MASMS_ENV=production
MASMS_SECRET_BACKEND=secrets_manager
MASMS_AWS_REGION=us-east-1
MASMS_SECRETS_PREFIX=masms/production
MASMS_DATABASE_URL=REPLACE_FROM_SECRETS_MANAGER
MASMS_CORS_ORIGINS=https://app.example.invalid
